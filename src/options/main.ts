/**
 * 设置页逻辑：API key 保存（chrome.storage.local）+ Excel box 导入（box.parseYituliuExcel）。
 */

// TODO(v0): apiKey 读写 + excelFile change → parseYituliuExcel → Box 存 chrome.storage.local
console.log("[Video2Team] options loaded");

export {};

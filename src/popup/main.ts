/**
 * Popup 面板逻辑：就绪检查（视频页 / box / API key）→ 触发分析 → 渲染 StageResult。
 */

// TODO(v0): 渲染 RecommendedSlot[] —— 每个槽位显示：
//   status（✓保留 / ⚠已替换 / ✗无解）+ 干员名 + 来源（L3/L2/AI）+ 替换类型 + 风险等级 + 溯源链接
console.log("[Video2Team] popup loaded");

export {};

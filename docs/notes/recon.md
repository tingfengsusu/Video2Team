# 侦察笔记：B站 API 与数据源（2026-09-13 验证）

## 1. B站接口验证结果 ✅ 全部可用

| 接口 | 端点 | 验证结论 |
|------|------|---------|
| 视频信息 | `GET https://api.bilibili.com/x/web-interface/view?bvid={BV}` | code 0。返回 aid、cid、title、desc（简介）、pubdate、**pages[]（分P列表，每P含 cid + part 标题）** |
| 评论 | `GET https://api.bilibili.com/x/v2/reply?type=1&oid={aid}&sort=1` | code 0，**无需 wbi 签名**。置顶在 `top` 字段（无置顶时为 null）；`replies[]` 含 rpid / like / content.message |
| 弹幕 | `GET https://comment.bilibili.com/{cid}.xml` | 直接返回 XML。`<d p="时间秒,模式,字号,颜色,...">文本</d>`，**每分P独立 cid** |

注意：请求应从插件 background 发出（host_permissions 已配置），携带用户登录态。

## 2. 关键发现：攻略合集存在三种形态

以 BV1q8FaekE9D（【相见欢】OR-EX-8突袭等EX关【摆完挂机】合集，15 分P）为样本：

| 形态 | 弹幕 | 评论 | 路由方案 |
|------|------|------|---------|
| a. 独立视频系列（一关一视频） | 天然隔离 | 天然隔离 | 无需路由（原设计） |
| **b. 多分P单一视频（实测常见！）** | **按分P cid 天然隔离** | **全合集混在一个评论区** | 分P标题（`view.pages[].part`，如「P3 OR-EX-2」）做正则路由 |
| c. 长视频不分P | 混杂 | 混杂 | 时间轴 + AI 识别关卡切换（v2 兜底，不变） |

→ 分析单元修订为「关卡」：独立视频 = 视频即关卡；分P = 分P即关卡。

## 3. 阵容提取的覆盖率提醒

样本 UP 的简介明确写「主要提供打法思路而非固定阵容搭配」，且该视频无置顶评论 → **简介/置顶评论文本路径并非 100% 覆盖**，抽帧 OCR 兜底（可选本地服务）的真实需求比预想高。v0 先做文本路径，无法提取时明确告知用户。

## 4. L1 干员字典：数据源确认 ✅

一图流开源仓库 `Arknights-yituliu/frontend-v2-plus` 的
`src/static/json/operator/character_table_simple.v2.json`：

- 顶层对象，key = charId（如 `char_145_prove`）
- 字段：`name`（干员名）、`profession`（职业）、`subProfessionId`（分支）、`rarity`、`skills[]`（技能名 + 专精材料）、`equip[]`（模块）
- 用途映射：名字典校验（name）+ 属性上下文（profession/branch/rarity）+ 技能名校验（skills[].skillName）
- 同目录 `composite_table.json`（升变/异格关联，如 星熊↔斩业星熊）值得一并利用

## 5. 新挑战：评论使用昵称/黑话缩写

实测评论原文：「借的 **ew** + 玛恩纳 + 余 硬是没打过，坐等单核」——社区大量使用缩写（ew、塞爹、42 等）。

方案：DeepSeek 精筛时**让 LLM 同时把昵称还原为干员全名**，再过字典校验全名（还原错会被字典拦住）。字典本体不建昵称索引，后续如需再补（PRTS 有昵称数据）。

## 7. Excel 练度表实测（2026-09-13，用户提供样例）✅ 格式确认

文件：单 sheet「干员练度表」，431 行 × 14 列：

```
干员名称 | 是否已招募(bool) | 星级 | 等级 | 精英化等级 | 潜能等级 | 通用技能等级
| 1技能专精等级 | 2技能专精等级 | 3技能专精等级 | χ分支模组 | γ分支模组 | Δ分支模组 | α分支模组
```

- **关键发现：导出的是全干员图鉴，431 行中仅 244 行「已招募」** → `owned` 过滤是 box 正确性的核心，不是可选项
- 数据质量：无重名、名称全 str、owned 全 bool，解析无需额外清洗
- 解析器已实现：`src/shared/box.ts`（按列名取值，关键列缺失时抛错带实际表头）

## 8. 测试视频确认（用户提供）

**BV18bYL65E54**（【月行水上】SR-EX-1至SR-EX-8突袭 摆完挂机 简单好抄）：

- 8 个分P，part 标题即关卡名（`SR-EX-1突袭`…`SR-EX-8突袭`）→ **分P路由方案坐实**；p=8 = SR-EX-8突袭，cid 41795649792
- **简介按职业写了完整常用干员阵容**（含技能/模组说明）→ 阵容提取文本路径的完美测试样本

v0 端到端测试素材：BV18bYL65E54（本视频）+ BV1q8FaekE9D（15分P、简介无阵容、评论含黑话——负样本兼挑战样本）。

## 9. 待办（需用户配合）

- [x] ~~用户提供一图流 Excel 样例~~ → 格式已确认，box.ts 已实现
- [x] ~~用户提供测试视频~~ → BV18bYL65E54 / BV1q8FaekE9D

## 10. 端到端验收实测（2026-09-13，SR-EX-8，BV18bYL65E54 P8）✅ 管道通，暴露核心缺陷

- 修复 2 个运行时 bug：①crxjs chunk 错位致 SW 崩溃（迁移 esbuild 解决）②评论 API `ps=49` 超界（实测上限 20）
- 管道全链路通：BV/分P 识别 → 阵容提取 → 弹幕/评论挖掘 → box 匹配 → 三色渲染（✓保留/⚠替换/✗无解+溯源）

**核心缺陷（用户实测指出）**：阵容提取走简介路径拿到的是「全合集常用干员」（40+人），**不是 SR-EX-8 的通关部署**——结果集对本关基本无用。真阵容在两种视频帧：

1. 编队页帧：「快捷编队/开始行动」UI，干员卡片+名字+助战位（德克萨斯/斩业星熊/蕾缪安/遥/黍/娜仁图亚/圣聆初雪/阿斯卡纶…）
2. 开局摆放帧：大字关卡名「SR-EX-8 虚无之顶」+ 部署顺序字幕「①遥↓②图亚↑③德→④安←⑤纶←⑥雪⑦理↑⑧泰⑨熊↑」

→ 帧提取升为主路径（设计文档 §4.1），本地分析服务从 v1 提前；文本路径降级为降级模式 + 显著警告。

**弹幕是替代建议的富矿（验收实拍）**：「老玛可以替askl」「askl|怎么替 好难啊」「这里谁能替蕾缪安」「大姨要用物伤替，别用酒神」「大姨可以换杰哥，伤害比较高」——密度远高于评论区。弹幕挖掘已接入 v0（正则粗筛入选，与评论合流送 LLM）。downkyi 下载器的 .ass 弹幕文件同源于 comment.bilibili.com/{cid}.xml，无需中转。
